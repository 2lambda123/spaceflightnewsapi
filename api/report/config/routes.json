{
  "routes": [
    {
      "method": "GET",
      "path": "/v3/reports/count",
      "handler": "report.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/v3/reports",
      "handler": "report.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/v3/reports/:id",
      "handler": "report.findOne",
      "config": {
        "policies": []
      }
    }
  ]
}
