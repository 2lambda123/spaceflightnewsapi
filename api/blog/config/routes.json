{
  "routes": [
    {
      "method": "GET",
      "path": "/v3/blogs/count",
      "handler": "blog.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/v3/blogs",
      "handler": "blog.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/v3/blogs/:id",
      "handler": "blog.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/v3/blogs/launch/:id",
      "handler": "blog.findPerLaunch",
      "config": {
        "policies": [],
        "description": "Get all blogs linked to a launch"
      }
    },
    {
      "method": "GET",
      "path": "/v3/blogs/event/:id",
      "handler": "blog.findPerEvent",
      "config": {
        "policies": [],
        "description": "Get all blogs linked to an event"
      }
    }
  ]
}
