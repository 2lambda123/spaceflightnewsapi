{
  "routes": [
    {
      "method": "GET",
      "path": "/v3/articles/count",
      "handler": "article.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/v3/articles",
      "handler": "article.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/v3/articles/:id",
      "handler": "article.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/v3/articles/launch/:id",
      "handler": "article.findPerLaunch",
      "config": {
        "policies": [],
        "description": "Get all articles linked to a launch"
      }
    },
    {
      "method": "GET",
      "path": "/v3/articles/event/:id",
      "handler": "article.findPerEvent",
      "config": {
        "policies": [],
        "description": "Get all articles linked to an event"
      }
    }
  ]
}
